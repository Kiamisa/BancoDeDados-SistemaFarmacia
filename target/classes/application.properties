# Configura��o do DataSource (Banco de Dados)
spring.datasource.url=jdbc:mysql://localhost:3306/farmacia_att
spring.datasource.username=root
spring.datasource.password=12345678
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configura��o do Flyway
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

# Configura��o do Hibernate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=none

# Configura��es adicionais (opcionais)
# spring.jpa.show-sql=true   # Exibe as queries geradas pelo Hibernate no console
# spring.jpa.properties.hibernate.format_sql=true   # Formata as queries para melhor visualiza��o
